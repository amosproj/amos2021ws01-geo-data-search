services:
  backend:
    build: ./src/backend
    ports:
      - 5000:8080
  nlp:
    build: ./src/nlp
    volumes:
      - ./src/nlp:/src
    ports:
      - 4000:8000
  frontend:
    build:
      context: ./src/frontend
      args:
        BACKEND_API_ROOT: http://localhost:5000/backend/user_query
    ports:
      - 8080:3000
    environment:
      - BACKEND_API_ROOT=http://localhost:5000/backend/user_query
